GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=
SESSION_SECRET=change-me-to-a-long-random-string
CLIENT_ORIGIN=http://localhost:5173
GITHUB_CALLBACK_URL=http://localhost:4000/auth/github/callback
PORT=4000

# On Vercel, CLIENT_ORIGIN / GITHUB_CALLBACK_URL default from VERCEL_URL
# when unset. For GitHub OAuth, also register the production callback URL
# on the OAuth App (https://<your-domain>/auth/github/callback).

